# Frontend (React + Chakra UI)

## Dev

```bash
cd frontend
npm install
npm run dev
```

Vite dev server proxies `/api` to `http://127.0.0.1:8000`.

## Build

```bash
cd frontend
npm run build
```

Build output goes to `frontend/dist`. FastAPI will serve it automatically.
